# TETO 1.6 记录模块验收验证报告

**对照文档**：[记录模块实现总结报告.md](./记录模块实现总结报告.md)（以下简称「实现总结报告」）§13 验收场景。

**验证日期**：2026-05-17

---

## 0. 验收定位

- 本报告用于验证《记录模块实现总结报告》所描述的 **当前代码实现** 是否与主链行为一致。
- **不**提出新功能、**不**修改代码或 SQL、**不**在本轮修复问题。
- 发现与报告不符或报告已记录的偏差，仅记入 **gap**（§13）与场景备注。
- 本轮以 **代码静态追踪** 为主；端到端 UI / 真实 DB / LLM 行为受环境限制处标注 **未测** 或 **未确认**。

---

## 1. 验收环境

| 项 | 记录 |
|----|------|
| Ingest V2 是否启用（逻辑上） | **未确认运行时**；按 `ingest-v2.ts`，若 `NODE_ENV=development` 或 `NEXT_PUBLIC_DEV_MODE=true` 且未显式关闭，则客户端/服务端默认 **开启** |
| `NEXT_PUBLIC_INGEST_V2` | **未确认**（`.env.local` 未纳入仓库，终端曾显示加载 `.env.local`） |
| `INGEST_V2` | **未确认** |
| `NEXT_PUBLIC_DEV_MODE` | **未确认** |
| `NODE_ENV` | 本地 `npm run dev` 时为 **development**（终端快照）；验收执行时 dev 进程 **未确认仍在运行** |
| 是否登录用户 | **未确认**；代码支持 `DEV_MODE` + `NEXT_PUBLIC_DEV_USER_ID` 或 Supabase 登录 |
| 测试日期 | 2026-05-17 |
| 浏览器环境 | **未测**（未执行 UI 手工测试） |
| 是否清空 sessionStorage | **未测** |
| 数据库 | **未确认**是否为本地/远程 Supabase；无本轮 SQL 查询记录 |
| API 探测 | 对 `http://localhost:3000/api/v2/inputs` 的 `curl` 探测 **无有效响应体**（服务未确认在线或沙箱外不可达） |

---

## 2. 验收方法

| 方式 | 使用范围 |
|------|----------|
| **代码静态确认** | 全部 9 个场景的主路径、状态枚举、字段写入 |
| API 请求测试 | **未测**（环境未稳定连通） |
| 手动 UI 测试 | **未测** |
| 数据库查询 | **未测** |
| trace/debug 页面 | **未测** |
| 控制台观察 | **未测** |

以下各场景 **验证方式** 默认含「代码静态确认」；若无额外标注，即未做运行时实测。

---

## 3. 验收场景一：普通直入库

**输入示例**：`今天下午跑步30分钟`

**验证方式**：代码静态确认（`QuickInput.tsx`、`inputs/route.ts`、`record-service.ts`）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 创建 `parsing` 会话卡 | 符合 | `handleSubmit` → `onPendingCreated`，`lifecycle: 'parsing'` |
| 请求 `POST /api/v2/inputs` | 符合 | `runIngestJob` 唯一主提交 API |
| 成功后移除 session 卡 | 符合 | 无 `pending` 时 `onPendingResolved` |
| 生成 `records` | 符合 | `!needsConfirmation` 分支循环 `createRecordSafely` |
| `input.status` → `completed` | 符合 | `inputs/route.ts` 257–260 行 |
| `input_unit.status` → `promoted` | 符合 | 250–254 行 |
| `review_status` → `confirmed` | 符合 | payload 212 行 |
| `confidence_level` → `high` | 符合 | 213 行 |
| `record_quality_tag` → `ai_high` | 符合 | 214 行 |

**场景结果**：**部分通过**

- **与实现总结报告一致**：是（直入库路径与 §7 表格一致）。
- **备注**：`needsConfirmation` 由 `classifyInput` + LLM/规则决定；**未实测**该例句是否一定无澄清。若模型返回 `finalIssues.length > 0`，会走澄清而非直入库。

**观察到的前端表现 / API / DB**：未测。

---

## 4. 验收场景二：普通字段澄清 answer

**建议输入**：缺事项或歧义事项（如仅「跑步 30 分钟」且未选手动事项，或触发 `item_missing` / `metric_prompt` 等）。

**验证方式**：代码静态确认（`clarification-planner.ts`、`inputs/route.ts`、`answer/route.ts`、`QuickInput.tsx`）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 返回 `pending` question | 符合 | `needsConfirmation` 时响应含 `pending` |
| 前端 `awaiting_confirmation` | 符合 | `onPendingSessionPatch` |
| 字段澄清 UI | 符合 | `kind` 为 text/number/datetime/select 且非复合四按钮 |
| answer 合并 `proposed_fields` | 符合 | `answer/route.ts` 77–84 行（`field !== '_confirm'`） |
| 生成 `record` | 符合 | answer 后 `createRecordSafely` |
| `record_quality_tag` → `clarified` | 符合 | answer 晋升 payload 170 行 |
| `confidence_level` → `medium` | 符合 | 169 行 |
| `input.status` completed | 符合 | 无待答 unit 时 `completed`；否则 `clarifying` |

**场景结果**：**部分通过**

- **与实现总结报告一致**：是。
- **备注**：触发哪一 clarifying 题依赖 **AI 清分结果**，本轮 **未构造真实 API 复现**。

---

## 5. 验收场景三：skip

**验证方式**：代码静态确认（`QuickInput.tsx` skip 按钮条件、`skip/route.ts`）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 「跳过此题」仅 `field !== '_confirm'` | 符合 | `QuickInput.tsx` 996 行 |
| skip 后立即 `createRecordSafely` | 符合 | `skip/route.ts` 94 行 |
| `unit.status` → `partial` | 符合 | 106 行 |
| `review_status` → `unchecked` | 符合 | 78 行 |
| `confidence_level` → `low` | 符合 | 79 行 |
| `record_quality_tag` → `partial` | 符合 | 80 行 |
| `input.status` → `partial` 或仍 `clarifying` | 符合 | 115–118 行按是否仍有 `pending_clarify` |

**场景结果**：**通过**（代码路径）

- **与实现总结报告一致**：是。
- **未测**：UI 点击与 DB 行级核对。

---

## 6. 验收场景四：cancel

**验证方式**：代码静态确认（`cancel/route.ts`、`QuickInput.tsx` `cancelIngestFlow`）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 前端会话卡 `cancelled` | 符合 | `onPendingSessionPatch({ lifecycle: 'cancelled' })` |
| `cancelled` 仅为前端会话态（相对 record） | 符合 | 未 promote 则无新 `records`；会话卡可仍留在 `pendingInputs` |
| 后端 `input.status` → `cancelled` | 符合 | `cancel/route.ts` 34 行 |
| `pending_clarify` / `ready` unit → `cancelled` | 符合 | 25–30 行 |
| 无正式 record（未 promote 前） | 符合 | cancel 不写 `createRecordSafely` |

**场景结果**：**通过**（代码路径）

- **与实现总结报告一致**：是（含 §13.4 补充：前端 `cancelled` ≠ 已生成正式 record）。

---

## 7. 验收场景五：defer

**输入建议**：`昨天跑步30分钟，晚上买了咖啡` → `compound_confirm` → 选 **defer**。

**验证方式**：代码静态确认（`answer/route.ts`、`QuickInput.tsx`、`RecordsClient.tsx`）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| answer 返回 `deferred: true` | 符合 | `answer/route.ts` 116–127 行 |
| 不写 DB defer 专用状态 | 符合 | 早退，无 `updateInput` defer |
| 前端 `lifecycle` → `deferred` | 符合 | `submitIngestClarify` 460–468 行 |
| `clarifySnapshot` 保存 | 符合 | patch 含 `clarifySnapshot` |
| sessionStorage | 符合 | `teto_records_pending_inputs_v1`、`teto_records_ingest_clarify_v1` |
| 刷新后同浏览器、storage 未清理可恢复 | 符合 | `loadPendingDraftsFromStorage` + 点击会话卡 `resumeClarify` |

**场景结果**：**部分通过**

- **与实现总结报告一致**：是。
- **备注**：复合句是否出现 `compound_confirm` 依赖 `isCompound && units.length > 1` 或启发式（`classify-input.ts` 509–546 行），**未实测**该输入一定触发；defer 按钮仅在复合四按钮 UI 出现。

---

## 8. 验收场景六：复合句 split

**验证方式**：代码静态确认（`answer/route.ts`、`clarification-planner.ts`）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 触发 `compound_confirm` | 条件符合代码 | 多 unit 复合或 `compound_uncertain` 议题 |
| 选 split 后多条 `records` | 符合 | `splitConfirmed` 批量 promote `ready` units |
| `promoted_record_ids` 多条 | 符合 | 响应 `allPromotedIds` |
| 共享 `batch_id` | 符合 | 149 行 `crypto.randomUUID()` |
| `record_links` `derived_from` | 符合 | 291–298 行 |
| units → `promoted` | 符合 | 268–272 行 |

**场景结果**：**部分通过**

- **与实现总结报告一致**：是。
- **未测**：真实 LLM 拆分条数、链接表行数据。

---

## 9. 验收场景七：复合句 keep_single

**验证方式**：代码静态确认（`inputs/route.ts` metadata、`answer/route.ts`）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| `compound_confirm` | 同场景六 | 未实测触发 |
| 仅晋升 `primary_unit_id` | 符合 | 138–142、194–211 行 |
| 其余 unit `cancelled` | 符合 | 277–287 行 |
| 仅一条主 record（keep 路径） | 符合 | 单次 `createRecordSafely` 针对 promoteUnit |
| `metadata.primary_unit_id` | 符合 | POST inputs 164–168 行默认 `unit_index === 0` |

**场景结果**：**部分通过**

- **与实现总结报告一致**：是。

---

## 10. 验收场景八：API 失败 failed 卡

**验证方式**：代码静态确认（`ingest-v2.ts`、`QuickInput.tsx`、`inputs/route.ts`、`RecordItem.tsx`）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 关闭 Ingest V2 时前端 failed/阻断 | 符合 | `resolveIngestV2ForClient` false → `onError`，不请求 |
| 请求失败时会话 `failed` | 符合 | `onPendingSessionPatch({ lifecycle: 'failed', errorMessage })` |
| 错误文案展示 | 符合 | `toPendingRecord` → `note`；`RecordItem`「失败」 |
| 无正式 record | 符合 | POST 失败不 promote |
| `INPUT_INGEST_DISABLED` | 符合 | 服务端 403 + 该错误码 |

**场景结果**：**部分通过**

- **与实现总结报告一致**：是。
- **未测**：未在本轮临时改环境变量复现 403；未查 `trace_summaries` 表。

---

## 11. 验收场景九：selectedDate 非今天的时间解析 gap

**验证方式**：代码静态确认（`QuickInput.tsx` 652–664 行、`inputs/route.ts` 79、88 行）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 发送 `metadata.date` | 符合 | `date: capturedDate` |
| 未发送 `body.date` | 符合 | body 无 `date` 字段 |
| classify 使用 `body.date \|\| server today` | 符合 | 第 79 行传入 `ingestFull` |
| `occurred_at` / `time_anchor_date` 可能与 UI 不一致 | 符合（风险） | 与实现总结报告 §8.3 一致 |
| 与报告 gap 描述一致 | 符合 | 逻辑 gap 成立 |

**场景结果**：**通过**（gap 静态复现）

- **与实现总结报告一致**：是。
- **未测**：跨日 UI 下真实 `records` 字段时间值。

---

## 12. 总体验收结论

| 场景 | 结果 | 是否与实现总结报告一致 | 是否发现新 gap |
|------|------|------------------------|----------------|
| 一、普通直入库 | 部分通过 | 是 | 否（依赖 LLM 是否澄清） |
| 二、字段澄清 answer | 部分通过 | 是 | 否 |
| 三、skip | 通过 | 是 | 否 |
| 四、cancel | 通过 | 是 | 否 |
| 五、defer | 部分通过 | 是 | 否 |
| 六、复合句 split | 部分通过 | 是 | 否 |
| 七、keep_single | 部分通过 | 是 | 否 |
| 八、API 失败 failed | 部分通过 | 是 | 否 |
| 九、selectedDate gap | 通过 | 是 | 否（报告已记录） |

**汇总**：9 场景中 **3 项** 代码路径 **通过**，**6 项** **部分通过**（缺运行时/UI/DB/LLM 实测），**0 项** **未通过**，**0 项** **未测**（均有静态结论）。

**结论**：实现总结报告与 **当前源码主链一致**；端到端行为需在具备 Supabase + LLM 的环境中补做 **手工或 API 回归** 后，方可将「部分通过」升为「通过」。

---

## 13. 发现的 gap

本轮 **未发现** 实现总结报告未记载的新逻辑 gap。下列为 **已确认仍成立**、仅作验收记录（不修复）：

| gap 名称 | 触发条件 | 当前表现 | 预期（产品/流程图） | 影响范围 | 后续任务 |
|----------|----------|----------|---------------------|----------|----------|
| selectedDate 与 classify 日期不一致 | UI 选非今天且未传 `body.date` | `ingestFull` 用服务器当天；`metadata.date` 为所选日 | 清分锚点与所选日历一致 | 相对时间解析、记录展示日 | 建议单开修复任务 |
| 直入库/澄清依赖 LLM | 任意自然语言输入 | `needsConfirmation` 非确定性 | 验收用例稳定可复现 | 场景一、二、六、七实测 | 建议 E2E 用 mock 或固定 eval 集 |
| 复合句触发不确定 | 单句多事件但模型未标 compound | 可能直入库或多 unit 无 `compound_confirm` | 稳定出现四按钮确认 | 复合句验收 | 文档已说明；实测需固定输入集 |

---

## 14. 建议下一步

1. **文档收口（推荐）**  
   - 9 场景与实现总结报告 **静态一致**，无新 gap。  
   - 可将《记录模块实现总结报告》标为 **「与代码对齐（静态验收 2026-05-17）」**。  
   - 本验收报告保留「部分通过」标注，提醒补 **运行时回归**。

2. **补测清单（不写代码）**  
   - 在 dev 环境确认 `NEXT_PUBLIC_INGEST_V2` / `DEV_MODE` 后，按 §13 各场景做 **UI + Network + Supabase 行查询** 各 1 次。  
   - 场景九：选昨日日期页提交「昨天下午跑步」，对比 `records.occurred_at` 与 `input.metadata.date`。  
   - 场景八：临时设 `NEXT_PUBLIC_INGEST_V2=false` 验证 failed 卡与 403（测完恢复配置）。

3. **若运行时有关键链路失败**  
   - 再开 **《记录模块验收问题修复清单》** 文档（仍可不改代码，只列问题单）。  
   - 当前 **不建议** 因静态验收已通过而直接进入代码修复。

---

**说明**：本报告记录 **2026-05-17 验证当时** 的环境与结论；补测后应更新 §1、§12 与各场景「未测」项。
